[build]
builder = "DOCKERFILE"
dockerfilePath = "currency-service/Dockerfile"
watchPatterns = ["currency-service/**", "cerps-common/**", "pom.xml"]

[deploy]
healthcheckPath = "/actuator/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
